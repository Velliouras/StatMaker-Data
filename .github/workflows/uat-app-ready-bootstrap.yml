name: Bootstrap UAT App-Ready Bundles

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived-bootstrap:
    runs-on: ubuntu-latest
    steps:
      - name: Artifact producer status
        run: echo "UAT app-ready bundles are already published; use the dedicated publisher for regeneration."
