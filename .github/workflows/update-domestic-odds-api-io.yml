name: Update Domestic Odds-API.io Odds

on:
  workflow_dispatch:
    inputs:
      mode:
        description: "Run scope: league, group, or all"
        required: true
        default: "all"
        type: choice
        options:
          - league
          - group
          - all
      target:
        description: "League code such as NOR, group such as scandinavia, or all_initial"
        required: false
        default: "all_initial"
        type: string
      dry_run:
        description: "Run without API calls and write structural JSON/report"
        required: true
        default: false
        type: boolean

permissions:
  contents: write

jobs:
  update-domestic-odds-api-io:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Fetch Domestic Odds-API.io odds
        env:
          ODDS_API_IO_KEY: ${{ secrets.ODDS_API_IO_KEY }}
          ODDS_API_IO_BOOKMAKERS: ${{ vars.ODDS_API_IO_BOOKMAKERS || 'Bet365,Unibet' }}
        run: |
          args=(--mode "${{ inputs.mode }}" --target "${{ inputs.target }}")
          if [ "${{ inputs.dry_run }}" = "true" ]; then
            args+=(--dry-run)
          fi
          python scripts/update_domestic_odds_api_io.py "${args[@]}"

      - name: Commit updated domestic odds JSON
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add odds/odds_api_io/domestic_odds.json reports/domestic_odds_debug.json
          if git diff --cached --quiet; then
            echo "No domestic odds changes to commit."
          else
            git commit -m "Update Domestic Odds-API.io odds"
            git push
          fi
